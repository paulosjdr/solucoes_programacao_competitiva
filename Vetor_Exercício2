#include <bits/stdc++.h>

using namespace std;

int main()
{
    vector<int> valor;

    int n;
    cin >> n;

    for(int i=0; i<n; i++){
        int temp;
        cin >> temp;
        valor.push_back(temp);
    }
    for(int i=0; i<n; i++){

    cout << valor[i] << endl;

    }

    return 0;
}
