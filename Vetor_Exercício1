#include <bits/stdc++.h>

using namespace std;

int main()
{
    vector<int> valores;

    for(int i=0; i<5; i++){

        int temp;

        cin >> temp;
        valores.push_back(temp);

    }

    sort(valores.begin(),valores.end());

    for(int i=0; i<5; i++){
        cout << valores[i] << endl;

    }

    return 0;
}
